# Environment / secrets — never commit real keys
.env
*.env
.env.*
!.env.example

# Python bytecode
__pycache__/
*.py[cod]
*.pyo

# Virtual environments
venv/
.venv/
env/

# macOS
.DS_Store

# IDE
.idea/
.vscode/

# yfinance / requests caches
*.cache

# SQLite cache (filings + LLM analyses + run history)
cache.db
cache.db-journal
cache.db-wal
cache.db-shm
